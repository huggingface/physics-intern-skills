---
name: survey
description: Survey the research landscape for the problem in problem.md. Produces survey.md covering background, known approaches, pitfalls, and key references. Provisional orientation — later evidence overrides it.
---

# Survey

Run the `/survey` workflow. The slash command expands the full workflow instructions in the active session.

Agents used: `surveyor`.

Output: `survey.md`, plus optional `references/<id>.*` fetches.
