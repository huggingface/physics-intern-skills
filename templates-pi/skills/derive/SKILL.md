---
name: derive
description: Analytical derivation for a target claim or open question, in a fresh-context sub-agent that uses only what is dispatched via a brief file. Writes derivations/D-NNN.md with the derivation, assumptions, and sanity checks.
---

# Derive

Run the `/derive` workflow. The slash command expands the full workflow instructions in the active session.

Agents used: `deriver`.

Output: `derivations/D-NNN.md` (next available number). Brief written to `derivations/.briefs/D-NNN-brief.md` before dispatch.
